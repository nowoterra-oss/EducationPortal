# ============================================================
# EduPortal - Environment Configuration Template
# ============================================================
# Copy this file and rename to:
#   - .env.test for test environment
#   - .env.prod for production environment
#
# Then update the values according to your environment.
# ============================================================

# Database Configuration
# ======================
# MSSQL_SA_PASSWORD: SA password for SQL Server
# DB_HOST: Database server hostname/IP
#          - For test: use container name (mssql-test)
#          - For prod: use external server address
# DB_PORT: Database port (default: 1433)
# DB_NAME: Database name
# DB_USER: Database username
MSSQL_SA_PASSWORD=your_secure_password_here
DB_HOST=your-database-server.com
DB_PORT=1433
DB_NAME=EduPortalDb
DB_USER=sa

# JWT Configuration
# =================
# JWT_KEY: Must be at least 32 characters long
#          Generate with: openssl rand -base64 32
# JWT_ISSUER: Token issuer identifier
# JWT_AUDIENCE: Token audience identifier
JWT_KEY=YourSecureJwtKeyMustBe32CharsLong!!
JWT_ISSUER=EduPortalAPI
JWT_AUDIENCE=EduPortalClient

# Email Configuration (Optional)
# ==============================
# SMTP settings for sending emails
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_SENDER_EMAIL=noreply@yourdomain.com
SMTP_SENDER_NAME=EduPortal
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# ============================================================
# Environment-specific notes:
# ============================================================
# TEST Environment:
#   - API runs on port 8080
#   - MSSQL runs in Docker container
#   - DB_HOST should be 'mssql-test' (container name)
#
# PRODUCTION Environment:
#   - API runs on port 8081
#   - MSSQL runs on external server
#   - DB_HOST should be your production DB server
# ============================================================
